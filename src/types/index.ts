export interface User {
  email: string;
  password: string;
}

export interface StockData {
  date: string;
  close: number;
}

export interface LoginFormData {
  email: string;
  password: string;
}
